# Ключевые функции проекта Video Analyzer

## 🎬 Backend - Video Service
- **`processVideo(file)`** — обработка видео: извлечение аудио, транскрипция, кадры, анализ
- **`processVideoFromUrl(url)`** — скачивание и обработка видео по URL
- **`extractAudio()`** — извлечение аудио в OGG Opus (FFmpeg)
- **`transcribeAudio()`** — транскрипция через Yandex STT (синхронное/асинхронное)
- **`extractKeyframes()`** — извлечение 5 ключевых кадров
- **`generateScript()`** — генерация сценария на основе паспорта стиля

## 🤖 Backend - Yandex GPT Service
- **`analyzeVideoContent()`** — создание "Паспорта стиля" (структура, стиль речи, визуальный контекст, инсайты)
- **`generateScript()`** — генерация посекундного сценария в стиле автора
- **`analyzeVisualContext()`** — анализ визуального контекста по кадрам
- **`enhanceFrameDescription()`** — улучшение описаний кадров через GPT

## 🎤 Backend - Yandex STT Service
- **`transcribeAudio()`** — синхронное распознавание (до 1 МБ)
- **`transcribeAudioAsync()`** — асинхронное распознавание (до 4 часов, до 1 ГБ)
- **`waitForRecognition()`** — ожидание завершения асинхронного распознавания

## 👁️ Backend - Yandex Vision Service
- **`analyzeImage()`** — анализ изображения (текст, лица, классификация)
- **`describeImage()`** — создание описания кадра

## 💾 Backend - Yandex Storage Service
- **`uploadFile()`** — загрузка файла в Object Storage
- **`deleteFile()`** — удаление файла из Object Storage

## 📥 Backend - Downloader Service
- **`detectPlatform()`** — определение платформы (YouTube/TikTok/Instagram)
- **`downloadVideo()`** — проксирование запроса к Python микросервису

## 🌐 Backend - Video Controller
- **`POST /video/upload`** — загрузка и анализ видео файла
- **`POST /video/analyze-url`** — анализ видео по URL
- **`POST /video/generate-script`** — генерация сценария

## 🎨 Frontend - App Component
- **`handleUpload()`** — отправка видео на анализ (файл/URL)
- **`handleGenerateScript()`** — генерация нового сценария
- **`isValidUrl()`** — валидация URL платформ

## 🐍 Python Microservice
- **`download_video()`** — скачивание видео через yt-dlp
- **`GET /download`** — API эндпоинт для скачивания
- **`GET /health`** — health check

## 🔄 Поток обработки
**Загрузка файла:** Controller → VideoService → ExtractAudio → Transcribe → ExtractKeyframes → Vision → GPT → Паспорт стиля

**Анализ по URL:** Controller → DownloaderService → Python микросервис → VideoService → (далее как загрузка)

**Генерация сценария:** Controller → VideoService → GPT Service → Сценарий

## 📊 Статистика
- **~36 ключевых функций**
- **6 backend модулей**
- **Микросервисная архитектура**

