# Video Analyzer - AI-ассистент для анализа видео-контента

Проект для хакатона Grozplexity - AI-ассистент для анализа видео-контента и выявления "формул удержания".

## 🚀 Возможности

### Уровень 1: "Наблюдатель"
- Загрузка видео файлов
- **Скачивание видео по URL** (YouTube, TikTok, Instagram) через universalDownloader
- Извлечение аудиодорожки
- Транскрипция аудио в текст (Yandex Speech-to-Text, до 4 часов)
- Извлечение ключевых кадров

### Уровень 2: "Аналитик"
- Семантический анализ структуры ролика (Хук/Завязка/Основная часть/Кульминация/CTA)
- Анализ стиля речи (скорость, типичные фразы, эмоциональная окраска)
- Визуальный контекст (место действия, темп, ключевые элементы)
- Создание "Паспорта стиля" автора

### Уровень 3: Генерация контента
- Генерация нового сценария на основе "Паспорта стиля"
- Посекундный сценарий с описанием кадров и текста
- Полное соответствие авторскому стилю

## 🛠 Технологии

### Backend
- **NestJS** - фреймворк для Node.js
- **Yandex GPT API** - для анализа контента и генерации сценариев
- **Yandex Speech-to-Text API** - для транскрипции аудио (асинхронное распознавание до 4 часов)
- **Yandex Vision API** - для анализа изображений кадров
- **Yandex Object Storage** - для хранения временных файлов при асинхронном распознавании
- **FFmpeg** - для обработки видео
- **Multer** - для загрузки файлов
- **universalDownloader** - для скачивания видео с YouTube, TikTok, Instagram

### Frontend
- **React** + **TypeScript**
- **Vite** - сборщик
- **Axios** - HTTP клиент
- **Lucide React** - иконки

## 📦 Установка

### Требования
- Node.js 18+
- FFmpeg (установлен в системе)
- API ключ Yandex GPT (Yandex Cloud)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd video-analyzer
```

2. Установите зависимости:
```bash
# Установка зависимостей для всех проектов
npm run install:all

# Или отдельно:
cd backend && npm install
cd ../frontend && npm install
```

3. Настройте переменные окружения:

Создайте файл `backend/.env`:
```env
YANDEX_API_KEY=your_yandex_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here
FFMPEG_PATH=D:\Utils\ffmpeg\bin
PORT=3000
DOWNLOADER_URL=http://localhost:5000
```

**Примечание:** 
- `DOWNLOADER_URL` - URL universalDownloader сервиса (по умолчанию http://localhost:5000)

**Примечание:** 
- `YANDEX_API_KEY` - **обязателен** для работы системы
- `YANDEX_FOLDER_ID` - **обязателен**, ID каталога в Yandex Cloud
- Получите ключи на https://cloud.yandex.ru/ (см. `YANDEX_GPT_SETUP.md`)
- `FFMPEG_PATH` - путь к папке с FFmpeg (если FFmpeg не в системном PATH)
  - Windows: `D:\Utils\ffmpeg\bin` (или ваш путь)
  - Linux/Mac: обычно не нужен, если FFmpeg установлен через пакетный менеджер
- `YANDEX_API_KEY` и `YANDEX_FOLDER_ID` используются как для Yandex GPT, так и для Yandex Speech-to-Text

4. Убедитесь, что FFmpeg установлен:
```bash
ffmpeg -version
```

Если не установлен:
- **Windows:** Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) или используйте `choco install ffmpeg`
- **macOS:** `brew install ffmpeg`
- **Linux:** `sudo apt-get install ffmpeg` или `sudo yum install ffmpeg`

## 🚀 Запуск

### Разработка

Запустите бэкенд и фронтенд в отдельных терминалах:

**Терминал 1 - Backend:**
```bash
cd backend
npm run start:dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Приложение будет доступно:
- Frontend: http://localhost:5173
- Backend: http://localhost:3000

### Production

```bash
# Backend
cd backend
npm run build
npm run start:prod

# Frontend
cd frontend
npm run build
# Разверните dist/ на вашем веб-сервере
```

## 📖 Использование

1. Откройте приложение в браузере (http://localhost:5173)
2. Загрузите видео файл (MP4, MOV, AVI и т.д.)
3. Нажмите "Анализировать видео"
4. Дождитесь завершения анализа - вы увидите:
   - Транскрипцию видео
   - Паспорт стиля автора (структура, стиль речи, визуальный контекст, инсайты)
5. Введите тему для нового видео и нажмите "Сгенерировать сценарий"
6. Получите готовый посекундный сценарий в стиле автора

## 🎯 API Endpoints

### POST `/video/upload`
Загрузка и анализ видео файла
- **Body:** `multipart/form-data` с полем `video`
- **Response:** Объект с транскрипцией, кадрами и паспортом стиля

### POST `/video/generate-script`
Генерация сценария на основе паспорта стиля
- **Body:** 
  ```json
  {
    "topic": "Тема видео",
    "stylePassport": { ... }
  }
  ```
- **Response:** 
  ```json
  {
    "script": "Готовый сценарий..."
  }
  ```

## 🔧 Настройка Google APIs

### Gemini API
1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создайте API ключ
3. Добавьте в `backend/.env`: `GEMINI_API_KEY=your_key`

### Google Speech-to-Text (опционально)
1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Speech-to-Text API
3. Создайте service account и скачайте JSON ключ
4. Добавьте путь к ключу в `backend/.env`: `GOOGLE_APPLICATION_CREDENTIALS=path/to/key.json`

**Примечание:** Без Speech-to-Text система будет работать в упрощенном режиме, используя только визуальный анализ.

## 📝 Структура проекта

```
video-analyzer/
├── backend/              # NestJS бэкенд
│   ├── src/
│   │   ├── video/       # Модуль обработки видео
│   │   ├── gemini/      # Модуль Gemini API
│   │   └── main.ts
│   └── package.json
├── frontend/            # React фронтенд
│   ├── src/
│   │   ├── App.tsx
│   │   └── App.css
│   └── package.json
└── README.md
```

## 🎨 Особенности UI

- Современный темный дизайн
- Адаптивная верстка
- Интуитивный интерфейс
- Визуализация результатов анализа
- Красивое отображение паспорта стиля

## ⚠️ Ограничения

- Максимальный размер файла: 100MB
- Поддерживаемые форматы: MP4, MPEG, MOV, AVI
- Для полной функциональности требуется настройка Google Speech-to-Text API
- Обработка видео по URL не реализована (только загрузка файлов)

## 🤝 Вклад

Это проект для хакатона Grozplexity. Для улучшений создавайте Issues и Pull Requests.

## 📄 Лицензия

MIT

## 👥 Авторы

Команда хакатона Grozplexity 2024

